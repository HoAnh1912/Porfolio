import styled from "styled-components";

export const MainLayoutWrapper = styled.div`
  height: 100vh;
  .cursor {
    background-color: #111;
    height: 32px;
    width: 32px;
    border-radius: 50%;
    position: fixed;
    top: 0;
    left: 0;
    pointer-events: none;
  }
`;
